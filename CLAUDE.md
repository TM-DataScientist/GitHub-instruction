# CLAUDE.md

このファイルは Claude Code がこのリポジトリで作業する際に従うべきルールを定義します。

## 仕様駆動開発のルール

このプロジェクトは仕様駆動開発を実践します。詳細なプロンプト集は [`docs/tools/prompts.md`](docs/tools/prompts.md) を参照してください。

### コード生成時のルール

1. **仕様ファイルを必ず参照**
   - コードを生成する前に、関連する仕様ファイル（`docs/spec/*.md` または `README.md`）を読む
   - 仕様が存在しない場合は、ユーザーに確認する

2. **仕様へのリンクをコメントに含める**
   - 生成したコードには、必ず仕様へのリンクをコメントで含める
   - 形式: `# 仕様: docs/spec/[ファイル名].md#[セクション名]`

3. **不明点は質問する**
   - 仕様が曖昧な場合は、コードを生成する前に質問する
   - 「適切に」「必要に応じて」などの表現があれば、具体化を求める

### 仕様記述時のルール

1. **Why（なぜ）を明確にする** — ビジネス的な背景や目的を含める
2. **What（何を）を具体的にする** — 曖昧な表現を避ける
3. **受入基準を測定可能にする** — テスト可能な形で記述する
4. **実装の詳細（How）は書かない** — コードで表現する

### Pull Request 作成時のルール

1. コードの変更に伴い、仕様ファイルも更新する
2. PR 説明文に関連する仕様へのリンクを含める
3. Issue 番号があれば記載する

### やってはいけないこと

1. 仕様なしにコードを書かない
2. 仕様とコードを分断させない（リンクを必ず含める）
3. 実装の詳細（How）を仕様に書かない
4. 曖昧な表現（「適切に」「必要に応じて」）を使わない
5. 受入基準を曖昧にしない

### 出力形式

- 仕様ファイル: GitHub Flavored Markdown
- コメント: 仕様へのリンクを必ず含める
- コミットメッセージ: Conventional Commits 形式（`feat:`, `fix:`, `docs:` など）
